#!/bin/bash
# start pulse just incase
pulseaudio --start
aout=$(amixer sget Master)
state=$(echo $aout | grep -P 'Front Right:' | grep -Po 'off')
perc=$(echo $aout | grep -P 'Front Right:' | grep -Po '\d{2,3}\%')
if [ -n "$state" ]
then
    echo ""
else
    echo " $perc"
fi
